#include<stdio.h>
int main (){
 int x;
    printf("entre the number :");
        scanf("%d", &x);
           int y;
    printf("entre the number :");
        scanf("%d", &y);
        int z;
    printf("entre the number :");
        scanf("%d", &z);

        printf("%d", (x+y+z)/3);
    return 0;
}